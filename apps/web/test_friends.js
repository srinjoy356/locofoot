const { createClient } = require('@supabase/supabase-js'); const dotenv = require('dotenv'); dotenv.config({path: '../../.env.local'}); dotenv.config({path: '../../.env'}); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function test() { const res = await supabase.from('friendships').select('*, requester:users!requester_id(id), addressee:users!addressee_id(id)').eq('status', 'ACCEPTED'); console.log(res); } test();  
